{
  "buildCommand": "npm run build --workspace @nomi/web",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/proxy?__nomi_path=:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
